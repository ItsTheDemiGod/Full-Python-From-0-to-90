# Membership operators
friends = 5

friends = friends + 1
friends += 1 # the above can be written as like this
friends = friends - 1
friends -= 1 # the above can be written as like this
friends = friends * 2
friends *= 1 # the above can be written as like this
friends = friends / 2
friends /= 2 # the above can be written as like this
friends = friends ** 2
friends **= 2 # the above can be written as like this

#print(friends)

x = 3.2456
y = -4.2345
z = 5

print(abs(y))
print(round(x))
print(round(y))
print(pow(x,2))
print(max(x,y))
print(min(x,y))
