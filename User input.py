# we use an input statement to take input from the user.
 # input(--------)
# if we want input of specific datatype, then we simply precede (data_type) before placing the input
# statement
#    str(input(------)) or int(input(-----)) or float(input(---)) or bool(input(----))


name = input("Enter your name: ")
print(f"Hello {name}, welcome to Python!")